/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    unoptimized: true,
  },
  // experimental: {
  //   instrumentationHook: true,
  // },
};

export default nextConfig;
